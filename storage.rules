// Storage rules — paste into Firebase Console → Storage → Rules
// Allow read for product images (bot + admin); restrict write in production.

rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /products/{allPaths=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
